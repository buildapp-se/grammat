# TODO: receptsajten

> Tvärprojekt-prio: se `../master-backlog.md`, uppdatera båda vid ändring.

Byggt och live 2026-07-07: sajt, worker, D1, konton, kvittolista, 17 startrecept, näringsvärde per portion, receptkategorier, hel-state backup/återställning. 2026-07-08: ingrediensbockning i receptvyn (grön bock till vänster, senast bockad överst i avbockat-blocket, utesluts ur listan), wake lock, helklickbara kort, multi-recept-import från AI, PWA-manifest med riktiga ikoner (icons/, "g."-ordmärke), "Dela receptet" via urklipps-JSON, designöversyn (senap ersatt av grönt #2E6B43, AA-kontrast på sekundär/överstruken text), "Gör en kopia" borttagen. Se PROJECT.md för arkitektur.

## Kvar / idéer
- [ ] **v2 (stora live-sajten)**: plan och vägval i `ARKITEKTUR.md` (2026-07-08). Beslut: inget hushåll (par delar konto, Firebase-kontolänkning ger Google + lösenord på samma konto), inga bilder, blob-modellen behålls. Fas 1 = Firebase Auth, sen publik flik + sparräknare (saves-tabell + recipes_index deriverad vid PUT), grupper med inbjudningslänk, PWA/Capacitor, engelska sist.
- [x] **Fas 1 deployad + live-verifierad 2026-07-08** (e-post, Google, kontolänkning testade i produktion av Patrik).
- [x] **Namnbyte under Konto** byggt 2026-07-08 (PUT /name i worker + fält under Konto, lokalt verifierat). Deployat, purgat och live-verifierat av Patrik (patzlofgren → patrik).
- [x] **Fas 2 deployad till Worker + D1 2026-07-08** (publik feed, `recipes_index`, `saves`, `grammat`-seed, reindex). Frontendfix samma session: Allas grupperas i en gemensam kurslista, ägare visas som "från X", `course` normaliseras, spara/ta bort använder `ownerId|id`.
- [x] Klickbar ägare/profilsida för skapade offentliga recept: deployat och prodverifierat 2026-07-08. När ett kort visar "från Julia" kan man klicka på Julia och se alla offentliga recept skapade av den användaren. Bygger på `recipes_index WHERE owner_id = ?`, inte på sparade recept. Form: `GET /users/:id/recipes` + `#/anvandare/:ownerId`, samma kort/gruppering som Allas recept.
- [x] **Fas 3 grupper deployad och prodtestad 2026-07-08 kväll** (worker `b0a8676b`, backup `recept-2026-07-08-203216.sql` togs först). Innehåller `groups`, `group_members`, `invites`, `GET /friends-feed`, `GET/POST /groups`, `POST /groups/:id/invite`, `POST /join/:code`, fliken `#/vanner`, join-route `#/join/:code` och gruppsektion under Konto. Patrik prodtestade: skapa grupp, länk, Julia joinade, grupp bildades.
- [x] **Fas 3-patch 2026-07-08 sen kväll** (worker `270557b8`, `app.js?v=groups2-20260708`): reindex-bugg fixad (sparade kopior med `src` indexerades som egna skapade recept och läckte till vänner-flödet), `DELETE /groups/:id` + ta bort-knapp med confirm, kopiera-ikon vid inbjudningslänken. API- och UI-verifierat lokalt.
- [ ] Städa remote `recipes_index` från 7 gamla src-kopior (kommando i HANDOFF.md Nästa steg, kräver Patriks godkännande), prodtesta sen att Vänners recept bara visar skapade recept.
- [ ] Google-consentskärmens branding: kosmetiskt, ingen funktionsbugg, inloggningen fungerar oavsett. Rutan visar just nu `grammat-78450.firebaseapp.com` istället för appens namn. OAuth-verifiering + egen authDomain, hör till lanseringsfasen. Utrett 2026-07-20: Website/HTTP-referrer-restriktion på API-nyckeln går INTE ihop med detta, Google Cloud Console vägrar lägga till `grammat-78450.firebaseapp.com` i website-listan (blockad Firebase-delad domän), och authDomain-bytet kräver en riktig custom-domän-uppsättning (Firebase Hosting + DNS), inte en konsol-toggle. buildapp.se är nu live (2026-07-21) som hemvist för sidoprojekten. Påbörjat 2026-07-21: Firebase Hosting-config förberedd i `recept/authhost/` (site `grammat-78450`, tom placeholder-sida), riktad mot planerad subdomän `auth.buildapp.se` eftersom apex redan är upptagen av GitHub Pages. Kvar: `firebase deploy --only hosting` (MCP-deploy-verktyget buggade, kör via CLI), lägg till `auth.buildapp.se` som custom domain i Firebase Console, DNS-verifiering + A-poster i Cloudflare (DNS-only, ej proxat), byt `authDomain` i index.html rad 155, testa Google-inloggning innan deploy till prod.
- [ ] Google-inloggningen (popup-flödet) kändes segt vid Patriks test 2026-07-20, direkt efter att API-nyckelns restriktioner ändrades. Inte verifierat om det var en engångsföreteelse (kvarvarande Google-propagering, ~5 min efter nyckeländring) eller ett återkommande mönster. Mät vid tillfälle: tid från klick på "Fortsätt med Google" till Googles kontoväljare visas, gärna några repetitioner.
- [ ] Legacy-PIN-koden (register/login/token-auth i worker + hopfällda formulär i app.js) rensas när alla befintliga konton är kopplade till Firebase (kolla: `SELECT name FROM users WHERE firebase_uid IS NULL`).
- [ ] Stående agentrutin: före riskabla ändringar, D1-migreringar eller deploy ska Codex/Claude köra D1-export till lokal `backups/` (`npx wrangler d1 export recept --remote --output backups/recept-YYYY-MM-DD-HHMMSS.sql`). `backups/` är git-ignored och får inte pushas/publiceras.
- [ ] (pausad 2026-07-08, Patrik vill inte ha fler just nu) Fler recept ur `recept 2.mht` konverterade till starter.json-format. 16 importerade totalt (11 st 2026-07-07, plus chiliräkor/avocadosalsa, Årengs Bloody Mary, Strawberry Daiquiri och Nubbesallad 2026-07-07). Kandidat kvar bl.a.: Höstgryta Irland, Gubbröra (finns bara som tom rubrik i mht, inget recept nedskrivet). Fullständig titellista på ~90 sidor i mht-filen finns inte sparad någonstans, kör om extraktionen i PROJECT.md om fler ska plockas ut. Nya starter-recept syns direkt för alla i "Allas recept", ingen per-konto-migrering behövs längre.
- [ ] Riktig verifiering i mobil/butik (planerad, "det gör vi sen"): logga in, wake lock-test (skärmen ska inte slockna i receptvyn), bocka ingredienser, handla med listan, ladda om mitt i, kolla synk på andra enheten. Full checklista i HANDOFF.md.
- [ ] Fyra av de nyimporterade recepten (salsiccia, räkpasta, chili con carne, gazpacho) saknar steg ("Inga steg nedskrivna") eftersom källan bara var video/länk, fyll på vid tillfälle.
- [ ] Ev. service worker för offline i butiken (PWA-manifest + hemskärmsikon klart 2026-07-08; SW kräver cache-versionering vid varje deploy, läggs till om täckningen i butiken faktiskt är dålig).

- [x] **Säkerhetsgranskning 2026-07-20** (worker.js, D1, Firebase-nyckel): hittade och fixade en läcka i legacy-endpointen `GET /allas-recept`, den läste rå state för alla användare och läckte recept markerade `private: true` till vem som helst med giltig inloggning (`/feed`, ersättaren, filtrerade redan bort private korrekt). Fix: hoppa över `r.private === true` i loopen, commit `9f3d5da`, deployad. Även begränsade Firebase-webbnyckeln (`AIzaSyCQGq...`) i Google Cloud Console till API restrictions = Identity Toolkit API + Token Service API (stänger risken att en skrapad nyckel missbrukas mot andra Google-API:er), Application restrictions lämnades på None (se raden ovan om varför Website-restriktion inte funkar).
- [x] **Fyra buggar 2026-07-24** (Patrik rapporterade efter att ha delat ett recept): (1) `GET /feed` krävde inloggning, så utloggade besökare bara såg starter.json och aldrig andras delade recept, trots att receptet inte var markerat privat, fix: `/feed` kräver inte längre Bearer-token. (2) Aktiv flik i navigeringen var otydlig (bara en tunn kantlinje skilde den från övriga, särskilt svårt på mobil/svag syn), fix: fet text + tydligare kontrast mot inaktiva flikar. (3) AI-importprompten sa åt AI:n att strippa tillagningsbeskrivningar ur ingrediensnamn (t.ex. "finhackad") för butiksvänlig summering, men infon försvann helt i stället för att hamna i ett steg, fix: prompten ber nu AI:n flytta sånt till rätt steg. (4) Näringsvärde: receptvyn visade bara ANTALET ingredienser utan data, inte VILKA, fix: listar namnen. La även till `findNutrient()`-fallback (matchar längsta nutrients-nyckel som helt ord i ingrediensnamnet, t.ex. "färskost" i "färskost med vitlök") plus en riktig `jalapeño`-post som saknades helt. Verifierat lokalt (worker dev + statisk server + Chrome), `node test.js` grönt.

## Kända begränsningar (medvetna)
- Sista skrivning vinner vid sync; ok eftersom varje lista har en ägare.
- Summering kräver identisk stavning av ingrediensnamn mellan recept.
- Ingen e-post, ingen självservice-PIN-återställning.
